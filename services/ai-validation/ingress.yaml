apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ai-validation
spec:
  ingressClassName: kong
  rules:
    - host: ai-validation.34.70.130.195.sslip.io
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ai-validation
                port:
                  number: 8000
