apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: api-recommendation
spec:
  ingressClassName: kong
  rules:
    - host: api-recommendation.34.70.130.195.sslip.io
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: api-recommendation
                port:
                  number: 8000
